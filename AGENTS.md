# Start here

Read `CONTINUE_HERE.md` first, then `AI_HANDOFF.md` and the original proposal.
The latest continuation note takes precedence over older pilot interpretations.

The user is a Filipino CS student. Communicate clearly in Taglish and explain
method choices before introducing complexity. The immediate goal is a trustworthy
feasibility pilot of non-semantic OCR-derived layout, not proving the thesis true.

Category matters. Universal or unseen-category transfer is not a required success
condition unless the user explicitly adopts that claim. Report category-wise results.
Do not use recognized text as classifier input. Treat detector confidence as a
quality diagnostic, not text quantity or arrangement.

Preserve historical outputs. Use versioned folders for methodology changes and
record assumptions, limitations, exact commands and outstanding work. Do not
silently reinterpret old feature names or overwrite historical results.

The user prefers to run long downloads and OCR jobs in a visible terminal; provide
commands for those jobs. Compact local analysis can run here. Do not commit virtual
environments, raw images or generated overlays. Polygon JSONs, manifests, feature
tables, code and reports are intended repository artifacts.
